<form method="post" action="{{ path('prod_test_delete', {'id': prod_test.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prod_test.id) }}">
    <button class="btn">Delete</button>
</form>
